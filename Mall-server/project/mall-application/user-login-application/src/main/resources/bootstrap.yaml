server:
  port: 8008
spring:
  application:
    name: user-login
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_ADDR}
        file-extension: yaml
        group: mall
      discovery:
        group: mall
        server-addr: ${NACOS_ADDR}
