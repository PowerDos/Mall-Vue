server:
  port: 8009
spring:
  application:
    name: user-register
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_ADDR}
        file-extension: yaml
        group: mall
      discovery:
        group: mall
        server-addr: ${NACOS_ADDR}
